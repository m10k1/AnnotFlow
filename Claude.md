# AnnotFlow プロジェクト開発ルール

あなたは「AnnotFlow」の専属AIエンジニアです。以下のルールを**いかなる例外もなく**厳守してコードを記述・修正してください。仕様書の詳細は `docs/spec.md`（または指定された仕様書ファイル）を参照してください。

## 1. パッケージ管理の絶対ルール
- **`uv` のみを使用すること。**
- `pip install` や `pip freeze` は絶対に使用しない。
- パッケージの追加は必ず `uv add <package>`（開発用は `uv add --dev <package>`）で行う。
- `requirements.txt` は作成せず、すべて `pyproject.toml` で管理する。

## 2. アーキテクチャとUIの分離（MVC厳守）
- `core/` ディレクトリ内のモジュール（データモデル、分割ロジック等）から、`PySide6` のUIクラス（`QMessageBox`、`QDialog`等）をインポート・呼び出すことは**絶対に禁止**。
- コアロジックとUIの橋渡しは、すべてシグナル、戻り値、またはデータクラスを介して行う。
- UIをブロックする可能性のある重い処理（ファイルI/O、計算）は、必ず `QThread` を継承したWorkerクラスを作成してバックグラウンドで実行する。

## 3. データと座標系の統一
- 内部データモデルのバウンディングボックス（`BoundingBox`）は、**「ピクセル単位の絶対座標（x_min, y_min, x_max, y_max）」** のみで保持する。
- COCOのセグメンテーションデータ（`segmentation`）は、変換・座標計算・バリデーションを一切行わず、パースした値をそのまま保持し、そのままエクスポートする。
- クラスIDはフォーマットに関わらず、必ず内部で「0始まり」に正規化して扱う。

## 4. UIスタイリングの制約
- ウィジェット個別に `setStyleSheet("color: red")` のようなインラインスタイルを記述することを禁止する。
- アプリ全体のスタイルは `assets/style.qss` に集約し、動的な状態変更は `setProperty` とQSSセレクタの組み合わせで制御する。

## 5. エラーハンドリングとスキップ
- インポート時に画像ファイルが見つからない場合、例外を送出して処理全体を中断してはならない。必ず該当レコードをスキップし、インポート完了後にUIへエラー一覧として返す設計にすること。

## 6. 開発プロセス（TDD）
- 実装は必ず「テストコードの作成」から始めること。
- `Phase 0`（テスト基盤とフィクスチャの作成）を完了し、テストが実行（および失敗）することを確認してから `Phase 1` の実装に進む。